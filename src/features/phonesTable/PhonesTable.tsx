import { FilterBar } from "../filterBar/FilterBar";
import { PropertyRows } from "../propertyRows/PropertyRows";

export function PhonesTable() {
  return (
    <main>
      <FilterBar />
      <PropertyRows />
    </main>
  );
}
